version: "2.0"
providers:
  kubernetes:
    enabled: true
    regions:
      - "default"
    services:
      - pods
      - deployments
output:
  default_format: "table"
database:
  path: "~/.corkscrew/db/corkscrew.duckdb"
